apiVersion: apps/v1
kind: Deployment
metadata:
  name: score-service
  namespace: {{ .Values.global.namespace }}
  labels:
    app: score-service
    chart: {{ .Chart.Name }}-{{ .Chart.Version }}
    {{- if .Values.monitoring.prometheusScrape }}
    prometheus.io/scrape: "true"
    prometheus.io/port: "{{ .Values.scoreService.port }}"
    {{- end }}
spec:
  replicas: {{ .Values.scoreService.replicaCount }}
  selector:
    matchLabels:
      app: score-service
  template:
    metadata:
      labels:
        app: score-service
    spec:
      containers:
      - name: score-service
        image: "{{ .Values.scoreService.image }}:{{ .Values.scoreService.tag }}"
        imagePullPolicy: {{ .Values.global.imagePullPolicy }}
        ports:
        - containerPort: {{ .Values.scoreService.port }}
        env:
        - name: MONGO_URI
          value: "mongodb://mongodb:27017/scoredb"
        - name: GRPC_PORT
          value: "{{ .Values.scoreService.port }}"
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"
---
apiVersion: v1
kind: Service
metadata:
  name: score-service
  namespace: {{ .Values.global.namespace }}
spec:
  ports:
  - port: {{ .Values.scoreService.port }}
    targetPort: {{ .Values.scoreService.port }}
    name: grpc
  selector:
    app: score-service